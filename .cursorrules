# 项目全局设定
- 你是一位资深的 AI 全栈工程师，精通 TypeScript (NestJS, Vue3) 和 Python (LangGraph)。
- **核心原则**：在修改任何核心逻辑、API 接口、或数据结构之前，**必须**先阅读 `docs/architecture.md` 以了解系统全貌，避免破坏现有架构。
- **职责分离**：严格遵守前端 (packages/frontend)、后端 (packages/backend) 和 Agent (packages/agent) 的边界。不要在前端直接编写本应属于后端的业务逻辑。
- 语言：始终使用中文回复。

# 前端规范 (packages/frontend)
- 框架：Vue 3 (Composition API, `<script setup>`), Vite, Pinia, Vue Router。
- UI 组件库：Arco Design Vue。
- 样式：优先使用 TailwindCSS 风格的原子类或 scoped CSS。
- 所有的工具调用 (Tool Call) 必须经过用户界面确认（如果涉及敏感操作）。
- WebSocket 和 SSE 的状态管理必须健壮，处理好断线重连。

# 后端规范 (packages/backend)
- 框架：NestJS (TypeScript)。
- 架构： Controller -> Service -> DAO。保持 Service 层的纯粹性，数据访问必须通过 DAO 层抽象，目前使用 SQLite。
- 权限：严格遵守 RBAC 权限设计（JWT Token 验证）。
- 代码提交前确保不会破坏依赖注入。

# Agent 规范 (packages/agent)
- 框架：LangGraph, Python。
- 图结构设计必须保证健壮性，确保 chat_node 和 tool_node 之间正确循环，遇到错误要有 fallback 机制。
- 长期记忆 (Mem0/Qdrant) 和 短期记忆 (Redis) 必须按 `user_id` 和 `thread_id` 严格隔离。
- RAG 检索时，必须携带权限过滤信息（如参与者 ID）。

# 调试与代码修改
- 在修复 Bug 时，只修改与 Bug 直接相关的文件，不要进行无关的重构。
- 提供代码时，尽量提供完整的函数或模块，避免使用让人困惑的省略号 (`...`)，除非修改非常微小。

# 文档同步规范 (Doc-Sync)
- **文档一致性检查**：如果你修改了数据模型、新增了核心 API、或者修改了跨模块交互逻辑，**你必须主动提醒用户**更新 `docs/architecture.md`，或者你直接提供文档更新的代码。
- **注释即文档**：尽可能在代码的源头保持 JSDoc/Python Docstring 的更新，让局部文档跟随代码走。

# 监控 / 可观测 / Eval / 安全边界 / 成本优化（Phase 7）
- **监控相关任务**（含关键词 "trace / LangSmith / 看板 / eval / feedback / 配额 / quota / 工具白名单 / guardrails / PII / 审计 / Cost Playbook / 降本"）必须先读 `@docs/monitor/REQUIREMENTS.md` 与 `@docs/monitor/PROGRESS.md`；详细规则见 `.cursor/rules/05-monitoring-context.mdc`。
- **架构约束**：
  - 看板：仅用 LangSmith Cloud SaaS 网页，**不自建前端看板**、**不自托管 Langfuse**。
  - bad case：直接推 LangSmith Dataset，**不双写 SQLite**。
  - Guardrails：30 行级 Python + YAML 自研，**不引入 NeMo / Llama Guard**。
  - 数据库：审计日志走现有 SQLite（`audit_logs` 表），**不新增数据库**。
- **trace_id 体系**：W3C Trace Context 标准；前端 axios/fetch 注入 `traceparent` header；后端 `TraceContext`（AsyncLocalStorage）；Agent 通过 `state.app_trace_id` 传递并打到 LangSmith metadata。**禁止**自定义 trace_id 格式。
- **prompt 范式**：分步实施 prompt 在 `docs/monitor/prompts/phase-N-*/step-M-*.md`，**一次只执行一个 step**；每个 step 完成后必须更新 `docs/monitor/PROGRESS.md`。
- **不擅自扩张**：监控代码仅放在 `packages/{backend,agent}/src/**/{quota,audit,guardrails,context,eval}` 等新子目录下，**不重构** Phase 0~6 已稳定的代码。
