---
description: 全项目通用编码规范和最佳实践
globs: "**/*"
alwaysApply: true
---

# 编码规范

## 通用规则
1. **中文注释**：所有注释和文档使用中文
2. **小步提交**：每完成一个独立功能点就可以提交
3. **错误处理**：不要吞掉错误，始终有合理的错误处理
4. **类型安全**：TypeScript 项目严格模式，Python 使用 type hints
5. **环境变量**：敏感信息一律走 .env，不硬编码

## 命名规范
- **文件名**：kebab-case (如 `user-service.ts`, `tool_registry.py`)
- **TypeScript 类**：PascalCase
- **TypeScript 函数/变量**：camelCase
- **Python 类**：PascalCase
- **Python 函数/变量**：snake_case
- **数据库表名**：snake_case 复数 (如 `users`, `roles`)
- **API 路由**：kebab-case 复数 (如 `/api/students`)

## Git 提交规范
```
feat: 新功能
fix: 修复
docs: 文档更新
refactor: 重构
chore: 杂项
```

## 禁止事项
- ❌ 不要使用 any 类型（TypeScript）
- ❌ 不要使用 console.log 做正式日志（用 Logger）
- ❌ 不要在代码中硬编码 API key、密码等敏感信息
- ❌ 不要一次性修改超过 3 个文件，保持变更集小而精
