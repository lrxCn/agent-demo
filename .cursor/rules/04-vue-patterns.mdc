---
description: Vue3 前端编码规范和组件设计模式
globs: "packages/frontend/**/*.{vue,ts,tsx}"
alwaysApply: false
---

# Vue3 前端编码规范

## 项目结构
```
packages/frontend/src/
├── views/              # 页面组件
│   ├── login/
│   ├── dashboard/
│   ├── user/           # 用户管理
│   ├── role/           # 角色管理
│   ├── student/        # 学生管理
│   ├── knowledge/      # 知识库
│   └── rtc/            # 语音通话
├── components/         # 通用组件
│   ├── layout/         # 布局组件
│   └── chat/           # AI 聊天浮窗
├── stores/             # Pinia 状态管理
│   ├── auth.ts         # 认证状态
│   ├── permission.ts   # 权限状态
│   └── chat.ts         # 聊天状态
├── router/             # 路由
│   ├── index.ts
│   ├── routes.ts       # 路由定义
│   └── guard.ts        # 权限守卫
├── api/                # API 请求
│   ├── request.ts      # axios 封装
│   └── modules/        # 按模块分类
├── composables/        # 组合式函数
│   ├── useChat.ts      # 聊天相关
│   ├── useWebRTC.ts    # WebRTC 相关
│   └── useToolRegistry.ts  # 前端工具注册
├── types/              # TypeScript 类型
└── utils/              # 工具函数
```

## 组件规范
- 使用 `<script setup lang="ts">` 语法
- 使用 Composition API，不使用 Options API
- Props 使用 defineProps 配合 TypeScript 类型
- Emits 使用 defineEmits 配合 TypeScript 类型

## Arco Design 使用
- 按需引入组件，不要全量引入
- 使用 Arco 的深色主题，科技感蓝紫渐变主色调
- 优先使用 Arco 内置组件，不要重复造轮子

## 状态管理 (Pinia)
- 每个独立领域一个 store
- store 中处理 API 调用和数据缓存
- 组件中不直接调用 API，通过 store

## 权限路由
```typescript
// 路由 meta 中定义所需权限
{
  path: '/students',
  meta: {
    requiresAuth: true,
    permissions: ['student:view'],
    title: '学生管理'
  }
}
// 导航守卫中检查权限
```

## 前端工具注册中心
```typescript
// 每个页面在 onMounted 时注册可用工具
// 在 onUnmounted 时注销
// 工具列表变化时通过 WebSocket 通知后端
const { register, unregister } = useToolRegistry()

onMounted(() => {
  register({
    name: 'create_student',
    description: '创建新学生',
    parameters: { /* JSON Schema */ },
    handler: async (params) => { /* 执行操作 */ },
    requireConfirm: true  // 需要用户确认
  })
})
```

## 禁止事项
- ❌ 不要使用 Options API
- ❌ 不要在组件中直接 fetch/axios，通过 api/ 层
- ❌ 不要硬编码路由权限，走 meta + guard
- ❌ 不要全量导入 Arco Design
