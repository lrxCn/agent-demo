# Phase 4 - Step 6: 对话触发学生 CRUD 端到端测试

## 上下文
工具注册中心和调用链路全部完成。进行端到端集成测试和修复。

## 任务

### 1. 端到端测试场景
确保以下场景全部通过：

**场景 1：对话查询**
- 在学生管理页面
- 聊天："帮我查一下有没有叫张三的学生"
- 预期：Agent 调用 query_students 工具 → 返回查询结果

**场景 2：对话创建（需确认）**
- 聊天："帮我创建一个学生，姓名王五，学号S099，男，二班"
- 预期：弹出确认框 → 确认后调用 API → 创建成功 → 列表刷新

**场景 3：对话删除（需确认）**
- 聊天："帮我删除学号S099的学生"
- 预期：弹出确认框 → 确认后删除 → 列表刷新

**场景 4：对话跳转（需确认）**
- 在任意页面
- 聊天："带我去学生管理"
- 预期：弹出确认 → 确认后跳转到 /students

**场景 5：用户拒绝**
- 聊天触发工具 → 弹出确认 → 点击取消
- 预期：Agent 收到取消反馈，回复"好的，已取消操作"

### 2. 修复发现的问题
根据测试结果修复 bug。

## 验证
以上 5 个场景全部通过。

## 完成后
更新 PROJECT_STATUS.md 标记 4-6 为 ✅，整个 Phase 4 完成。

```bash
git add .
git commit -m "feat: Phase 4 完成 - AI 对话集成（浮窗/流式/工具调用/按需注册）"
```
